2. Implement logic in the index controller by making it:
    a. add custom validator for the email in the db
    b. adding the user to the db.
    c. redirecting the user to it's profile.

3. implement the user profile view/controller (/profile).
4. populate forms that have errors with the previously submitted values.
5. try to send emails through gmail !


